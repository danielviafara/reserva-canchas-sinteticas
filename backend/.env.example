# Cadena de conexion a SQL Server (ajusta usuario, clave y servidor).
# El numero de "ODBC Driver" depende del driver instalado (17 o 18).
DATABASE_URL=mssql+pyodbc://sa:TuPassword_123@localhost:1433/ReservaCanchas?driver=ODBC+Driver+18+for+SQL+Server&TrustServerCertificate=yes

JWT_SECRET=cambia-esta-clave
JWT_ALGORITHM=HS256
